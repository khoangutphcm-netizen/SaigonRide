@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
<!DOCTYPE html>
<html lang="@(System.Globalization.CultureInfo.CurrentUICulture.Name)">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SaigonRide</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    
    <style>
         @@keyframes smoothFadeIn {
            0% { opacity: 0; transform: translateY(10px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        body { animation: smoothFadeIn 0.35s ease-out forwards; }
        :root {
            --sidebar-bg-start: #0a3b22;
            --sidebar-bg-end: #1b6e3c;
            --sidebar-hover: #2a8a4a;
            --main-bg: #f5f7f6;
            --card-bg: #ffffff;
            --text-main: #333333;
            --text-muted: #888888;
            --accent-green: #20994a;
        }

        body.dark-mode {
            --sidebar-bg-start: #051a0e;
            --sidebar-bg-end: #0d3d20;
            --sidebar-hover: #1a5c31;
            --main-bg: #121212;
            --card-bg: #1e1e1e;
            --text-main: #e0e0e0;
            --text-muted: #aaaaaa;
        }
 
        body { background-color: var(--main-bg); color: var(--text-main); font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; overflow: hidden; height: 100vh; margin: 0; }
 
        #wrapper { display: flex; height: 100vh; width: 100vw; overflow: hidden; }
         
        #sidebar { width: 230px; background: linear-gradient(135deg, var(--sidebar-bg-start) 0%, var(--sidebar-bg-end) 100%); color: white; display: flex; flex-direction: column; flex-shrink: 0; z-index: 1000; height: 100vh; overflow-y: auto; overflow-x: hidden; margin-left: -230px; position: relative; -ms-overflow-style: none; scrollbar-width: none; }
        #sidebar::-webkit-scrollbar { display: none; }
        
        #sidebar::before { content: ''; position: absolute; top: -50px; right: -50px; width: 150px; height: 150px; background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%); border-radius: 50%; pointer-events: none; z-index: 0; }
        #sidebar::after { content: ''; position: absolute; bottom: 20%; left: -50px; width: 200px; height: 200px; background: radial-gradient(circle, rgba(255,255,255,0.05) 0%, transparent 70%); border-radius: 50%; pointer-events: none; z-index: 0; }
        
        .user-deco-icon { position: absolute; color: rgba(255,255,255,0.1); pointer-events: none; z-index: 0; animation: userFloat 6s infinite ease-in-out; }
        .user-deco-1 { top: 10%; right: 20px; font-size: 1.8rem; animation-delay: 0s; }
        .user-deco-2 { top: 45%; left: 15px; font-size: 1.2rem; animation-delay: 2s; }
        .user-deco-3 { top: 75%; right: 30px; font-size: 2rem; animation-delay: 1s; }
        
        .user-deco-wave { position: absolute; bottom: 0; left: 0; width: 100%; height: auto; opacity: 0.1; pointer-events: none; z-index: 0; transform: scaleY(0.7); transform-origin: bottom; }
        
        @@keyframes userFloat { 0%, 100% { transform: translateY(0) rotate(0deg); } 50% { transform: translateY(-15px) rotate(10deg); } }

        .sidebar-brand, .sidebar-nav, .user-panel { position: relative; z-index: 1; }

        #sidebar.sidebar-open { margin-left: 0; }
        .sidebar-transition { transition: margin-left 0.3s ease-in-out; }
        .sidebar-brand { padding: 1.5rem; display: flex; align-items: center; gap: 10px; text-decoration: none; color: white; }
        .sidebar-nav .nav-link { color: rgba(255,255,255,0.8); padding: 0.8rem 1.5rem; display: flex; align-items: center; gap: 12px; font-weight: 500; border-radius: 0 25px 25px 0; margin-right: 1.5rem; transition: all 0.2s; }
        .sidebar-nav .nav-link:hover, .sidebar-nav .nav-link.active { background-color: var(--sidebar-hover); color: white; box-shadow: 0 2px 8px rgba(0,0,0,0.2); }
        
        #page-content { flex-grow: 1; display: flex; flex-direction: column; min-width: 0; height: 100vh; overflow: hidden; background: transparent; }
        
        .topbar { background: transparent !important; padding: 1rem 2rem; display: flex; justify-content: space-between; align-items: center; flex-shrink: 0; border: none !important; box-shadow: none !important; }
        .search-bar { background: var(--card-bg); border: 1px solid #e0e0e0; border-radius: 20px; padding: 0.4rem 1rem; display: flex; align-items: center; width: 300px; }
        .search-bar input { border: none; background: transparent; outline: none; margin-left: 8px; width: 100%; color: var(--text-main); }
        
        body.dark-mode .bg-white, body.dark-mode .card { background-color: var(--card-bg) !important; border-color: #333 !important; color: var(--text-main) !important; }
        body.dark-mode .search-bar { border-color: #333; }
        body.dark-mode .text-dark { color: #e0e0e0 !important; } 
        body.dark-mode .text-muted { color: var(--text-muted) !important; }
        body.dark-mode .border-bottom, body.dark-mode .border-end { border-color: #333 !important; }
        body.dark-mode .list-group-item { background-color: var(--card-bg) !important; color: var(--text-main) !important; border-color: #333 !important; }
        body.dark-mode .bg-light { background-color: #1a1d20 !important; color: var(--text-main) !important; }
        body.dark-mode .form-control, body.dark-mode .form-select { background-color: var(--main-bg) !important; color: var(--text-main) !important; border-color: #333 !important; }
        body.dark-mode .form-control:focus, body.dark-mode .form-select:focus { background-color: var(--card-bg) !important; color: var(--text-main) !important; border-color: #20994a !important; }
        body.dark-mode input[readonly] { background-color: #1e1e1e !important; }
    </style>
</head>
<body>
    <div id="wrapper">
        <aside id="sidebar">
            <i class="bi bi-cloud-fill user-deco-icon user-deco-1"></i>
            <i class="bi bi-geo-alt-fill user-deco-icon user-deco-2"></i>
            <i class="bi bi-bicycle user-deco-icon user-deco-3"></i>
            <svg class="user-deco-wave" viewBox="0 0 1440 320" xmlns="http://www.w3.org/2000/svg">
                <path fill="#ffffff" fill-opacity="1" d="M0,160L48,170.7C96,181,192,203,288,197.3C384,192,480,160,576,165.3C672,171,768,213,864,213.3C960,213,1056,171,1152,149.3C1248,128,1344,128,1392,128L1440,128L1440,320L1392,320C1344,320,1248,320,1152,320C1056,320,960,320,864,320C768,320,672,320,576,320C480,320,384,320,288,320C192,320,96,320,48,320L0,320Z"></path>
            </svg>

            <a href="/" class="sidebar-brand mb-4 mt-2">
                <i class="bi bi-scooter fs-2 text-white"></i>
                <div>
                    <h4 class="fw-bold mb-0 text-white" style="letter-spacing: 0.5px;">SaigonRide</h4>
                    <small style="font-size: 0.7rem; color: rgba(255,255,255,0.7);">@Localizer["BrandSlogan"]</small>
                </div>
            </a>

            @{
                var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
                var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
            }
            
            <nav class="sidebar-nav flex-grow-1">
                <a asp-controller="Home" asp-action="Index" class="nav-link @(currentController == "Home" ? "active" : "")">
                    <i class="bi bi-house-door fs-5"></i> @Localizer["Menu_Home"]
                </a>
                <a asp-controller="Booking" asp-action="Index" class="nav-link @(currentController == "Booking" ? "active" : "")">
                    <i class="bi bi-bicycle fs-5"></i> @Localizer["Menu_Rent"]
                </a>
                <a asp-controller="History" asp-action="Index" class="nav-link @(currentController == "History" ? "active" : "")">
                    <i class="bi bi-clock-history fs-5"></i> @Localizer["Menu_History"]
                </a>
                <a asp-controller="Profile" asp-action="Support" class="nav-link @(currentController == "Profile" && currentAction == "Support" ? "active" : "")">
                    <i class="bi bi-headset fs-5"></i> @Localizer["Menu_Support"]
                </a>
                <a asp-controller="Profile" asp-action="Index" class="nav-link @(currentController == "Profile" && currentAction == "Index" ? "active" : "")">
                    <i class="bi bi-gear fs-5"></i> @Localizer["Menu_Settings"]
                </a>
            </nav>

            @if (User.Identity.IsAuthenticated)
            {
                <div class="user-panel px-3 pb-4">
                    <h6 class="text-uppercase mb-3" style="font-size: 0.75rem; color: rgba(255,255,255,0.6); padding-left: 1rem;">@Localizer["AccountLabel"]</h6>
                    <div class="d-flex align-items-center mb-4 px-3">
                        <div class="bg-success text-white rounded-circle d-flex justify-content-center align-items-center me-3" style="width: 40px; height: 40px; font-weight: bold;">
                            @{
                                var avatarUrl = User.FindFirst("AvatarUrl")?.Value;
                                var currentName = User.Identity.Name ?? "User";
                            }
                            @if (!string.IsNullOrEmpty(avatarUrl))
                            {
                                <img src="@avatarUrl" alt="Avatar" class="rounded-circle shadow-sm" style="width: 35px; height: 35px; object-fit: cover;" onerror="this.src='https://ui-avatars.com/api/?name=@currentName&background=0a3b22&color=fff'" />
                            }
                            else
                            {
                                <img src="https://ui-avatars.com/api/?name=@currentName&background=0a3b22&color=fff" alt="Avatar" class="rounded-circle shadow-sm" style="width: 35px; height: 35px; object-fit: cover;" />
                            }
                        </div>
                        <div style="line-height: 1.2;">
                            <span class="d-block fw-bold fs-6">@User.Identity.Name</span>
                            <small style="color: rgba(255,255,255,0.7); font-size: 0.75rem;">
                                @(User.FindFirst("UserType")?.Value == "Tourist" ? Localizer["UserType_Tourist"] : Localizer["UserType_Local"])
                            </small>
                        </div>
                    </div>

                    @if (User.FindFirst("UserType")?.Value == "Admin")
                    {
                        <div class="mt-4 mb-3">
                            <a class="btn w-100 fw-bold d-flex align-items-center justify-content-center gap-2" style="background-color: #ffcccc; color: #dc3545; border-radius: 12px; padding: 10px 5px; font-size: 0.95rem; text-decoration: none; box-shadow: 0 4px 6px rgba(0,0,0,0.1);" href="/AdminDashBoard/Index">
                                <i class="bi bi-shield-lock-fill fs-5"></i> Admin Dashboard
                            </a>
                        </div>
                    }

                    <a href="javascript:void(0);" onclick="localStorage.removeItem('theme'); document.body.classList.remove('dark-mode'); window.location.href='/Account/Logout';" 
                       class="btn w-100 fw-bold rounded-3 py-2 mt-2 d-flex align-items-center justify-content-center gap-2" 
                       style="background-color: rgba(255, 255, 255, 0.1); color: white; border: 1px solid rgba(255, 255, 255, 0.2); transition: all 0.2s;" 
                       onmouseover="this.style.backgroundColor='rgba(220, 53, 69, 0.9)'; this.style.borderColor='rgba(220, 53, 69, 1)';" 
                       onmouseout="this.style.backgroundColor='rgba(255, 255, 255, 0.1)'; this.style.borderColor='rgba(255, 255, 255, 0.2)';">
                        <i class="bi bi-box-arrow-left fs-5"></i> @Localizer["BtnLogout"]
                    </a>
                </div>
            }
        </aside>

        <div id="page-content">
            <header class="topbar">
                <button id="sidebarToggle" class="btn fs-4 text-dark p-0" style="background: transparent; border: none; box-shadow: none;">
                    <i class="bi bi-list"></i>
                </button>
                
                <div class="d-flex align-items-center gap-3 ms-auto">
                    <div class="dropdown">
                        <button class="btn fw-bold d-flex align-items-center gap-2 text-dark" style="background: transparent; border: none; box-shadow: none;" data-bs-toggle="dropdown">
                            <i class="bi bi-globe2 text-success"></i> 
                            @(System.Globalization.CultureInfo.CurrentUICulture.Name.StartsWith("en") ? "EN" : "VI") 
                            <i class="bi bi-chevron-down ms-1" style="font-size: 0.7rem;"></i>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end shadow border-0 rounded-3">
                            <li>
                                <form asp-controller="Language" asp-action="SetLanguage" method="post" id="langFormVi" class="m-0">
                                    <input type="hidden" name="returnUrl" value="@Context.Request.Path@Context.Request.QueryString" />
                                    <input type="hidden" name="culture" value="vi" />
                                    <button type="button" onclick="submitLanguageForm('langFormVi')" class="dropdown-item fw-bold text-success">🇻🇳 Tiếng Việt</button>
                                </form>
                            </li>
                            <li>
                                <form asp-controller="Language" asp-action="SetLanguage" method="post" id="langFormEn" class="m-0 mt-1">
                                    <input type="hidden" name="returnUrl" value="@Context.Request.Path@Context.Request.QueryString" />
                                    <input type="hidden" name="culture" value="en" />
                                    <button type="button" onclick="submitLanguageForm('langFormEn')" class="dropdown-item text-muted">🇬🇧 English</button>
                                </form>
                            </li>
                        </ul>
                    </div>

                    @if (User.Identity.IsAuthenticated)
                    {
                        <div class="dropdown">
                            <button class="btn d-flex align-items-center justify-content-center position-relative text-dark p-2" style="background: transparent; border: none; box-shadow: none;" data-bs-toggle="dropdown">
                                <i class="bi bi-bell fs-5"></i>
                                <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" id="notificationBadge" style="font-size: 0.6rem; display: none;">
                                    <span id="notificationCount">0</span>
                                </span>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end shadow border-0 rounded-3 mt-2" style="min-width: 350px;">
                                <li class="dropdown-header fw-bold text-dark">@Localizer["NotifTitle"]</li>
                                <li><hr class="dropdown-divider"></li>
                                <li id="notificationList">
                                    <a class="dropdown-item text-muted text-center py-3"><small>@Localizer["NotifEmpty"]</small></a>
                                </li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-center fw-bold text-success" href="/Reports/MyReports">@Localizer["ViewMyReports"]</a></li>
                             </ul>
                        </div>

                        <div class="dropdown">
                            <button class="btn d-flex align-items-center gap-2 p-1 pe-2" style="background: transparent; border: none; box-shadow: none;" data-bs-toggle="dropdown">
                                <div class="bg-success text-white rounded-circle d-flex justify-content-center align-items-center shadow-sm" style="width: 30px; height: 30px;">
                                    @{
                                        var avatarUrl = User.FindFirst("AvatarUrl")?.Value;
                                        var currentName = User.Identity.Name ?? "User";
                                    }
                                    @if (!string.IsNullOrEmpty(avatarUrl))
                                    {
                                        <img src="@avatarUrl" alt="Avatar" class="rounded-circle" style="width: 30px; height: 30px; object-fit: cover;" onerror="this.src='https://ui-avatars.com/api/?name=@currentName&background=0a3b22&color=fff'" />
                                    }
                                    else
                                    {
                                        <img src="https://ui-avatars.com/api/?name=@currentName&background=0a3b22&color=fff" alt="Avatar" class="rounded-circle" style="width: 30px; height: 30px; object-fit: cover;" />
                                    }
                                </div>
                                <div class="text-start" style="line-height: 1;">
                                    <span class="d-block fw-bold text-dark" style="font-size: 0.8rem;">@User.Identity.Name</span>
                                    <small class="text-muted" style="font-size: 0.7rem;">
                                        @(User.FindFirst("UserType")?.Value == "Tourist" ? Localizer["UserType_Tourist"] : Localizer["UserType_Local"])
                                    </small>
                                </div>
                                <i class="bi bi-chevron-down ms-1 text-muted" style="font-size: 0.7rem;"></i>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end shadow border-0 rounded-3 mt-2">
                                <li><a class="dropdown-item py-2 text-danger fw-bold" href="javascript:void(0);" onclick="localStorage.removeItem('theme'); document.body.classList.remove('dark-mode'); window.location.href='/Account/Logout';"><i class="bi bi-box-arrow-right me-2"></i>@Localizer["BtnLogout"]</a></li>
                            </ul>
                        </div>
                    }
                </div>
            </header>

            <main class="p-4" style="flex: 1; overflow-y: auto;">
                @RenderBody()
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        if(localStorage.getItem('theme') === 'dark') { document.body.classList.add('dark-mode'); }
        
        function submitLanguageForm(formId) {
            var langForm = document.getElementById(formId);
            var cultureValue = langForm.querySelector('input[name="culture"]').value;
            
            var paymentForm = document.getElementById('paymentForm') || document.getElementById('rentForm');
            if (paymentForm) {
                var returnUrlInput = document.createElement('input');
                returnUrlInput.type = 'hidden';
                returnUrlInput.name = 'returnUrl';
                returnUrlInput.value = window.location.pathname + window.location.search;
                var cultureInput = document.createElement('input');
                cultureInput.type = 'hidden';
                cultureInput.name = 'culture';
                cultureInput.value = cultureValue;

                paymentForm.appendChild(returnUrlInput);
                paymentForm.appendChild(cultureInput);

                var originalAction = paymentForm.action;
                paymentForm.action = '/Language/SetLanguage';
                paymentForm.submit();
                
                paymentForm.action = originalAction;
            } else {
                langForm.submit();
            }
        }

        document.addEventListener('DOMContentLoaded', function() {
            loadNotifications();
            setInterval(loadNotifications, 30000); 
        });
        
        async function loadNotifications() {
            try {
                const response = await fetch('/api/notifications/count');
                const data = await response.json();

                if (data.count > 0) {
                    document.getElementById('notificationBadge').style.display = 'inline-block';
                    document.getElementById('notificationCount').textContent = data.count;

                    const listResponse = await fetch('/api/notifications/list');
                    const notifications = await listResponse.json();
                    let listHTML = '';
                    const statusVi = { 'Open': 'Mới', 'InProgress': 'Đang xử lý', 'Resolved': 'Đã giải quyết', 'Closed': 'Đóng' };
                    const statusEn = { 'Open': 'Open', 'InProgress': 'In Progress', 'Resolved': 'Resolved', 'Closed': 'Closed' };
                    notifications.forEach(notif => {
                        listHTML += `<a class="dropdown-item py-2" href="/Reports/ReportDetails/${notif.reportId}">
                            <div class="d-flex gap-2">
                                <i class="bi bi-info-circle text-info mt-1"></i>
                                <div>
                                    <small class="fw-bold">${notif.title}</small><br>
                                    <small class="text-muted">${notif.status}</small>
                                </div>
                            </div>
                        </a>`;
                    });

                    document.getElementById('notificationList').innerHTML = listHTML;
                } else {
                    document.getElementById('notificationBadge').style.display = 'none';
                }
            } catch (error) {
                console.error('Error loading notifications:', error);
            }
        } 
        
        document.addEventListener('DOMContentLoaded', function() {
            var sidebarToggle = document.getElementById('sidebarToggle');
            var sidebar = document.getElementById('sidebar');

            if (sidebarToggle && sidebar) {  
                var savedState = localStorage.getItem('sidebarState');
                if (savedState === 'open') {
                    sidebar.classList.add('sidebar-open');
                } else if (savedState === 'closed') {
                    sidebar.classList.remove('sidebar-open');
                } 
                 
                setTimeout(function() {
                    sidebar.classList.add('sidebar-transition');
                }, 50);
                 
                sidebarToggle.addEventListener('click', function(e) {
                    sidebar.classList.toggle('sidebar-open'); 
                     
                    if (sidebar.classList.contains('sidebar-open')) {
                        localStorage.setItem('sidebarState', 'open');
                    } else {
                        localStorage.setItem('sidebarState', 'closed');
                    }
                    e.stopPropagation(); 
                });
            }
        });
    </script>
</body>
</html>